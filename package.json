{
  "name": "to-do",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "if-env TYPE=server && (cd server && npm install) || (cd client && npm start)",
    "server": "node ./server/server.js",
    "client": "cd client && npm start"
  },
  "author": "AkaOm",
  "license": "ISC",
  "dependencies": {
    "if-env": "^1.0.4"
  }
}
